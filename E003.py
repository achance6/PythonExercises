# Asks user to enter width and length, computes area
width = float(input("Width in feet: "))
length = float(input("Length in feet: "))
print("Area of those measurements is", "%.2f" % (width * length), "feet")